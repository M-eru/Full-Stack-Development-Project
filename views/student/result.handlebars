<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #96D4D4;">
    <div class="navbar-collapse collapse w-100 order-1 order-md-0 creationNav mx-2 my-2">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link text-decoration-none text-dark" data-toggle="modal" data-target="#warningModal"><i
                        class='fas fa-angle-left'></i></a>
            </li>
        </ul>
    </div>
    <div class="mx-auto order-0">
        {{#with data}}
        <a class="navbar-brand mx-auto" href="#">Results for {{tutName}}</a>
        {{/with}}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".creationNav">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 creationNav">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
            </li>
        </ul>
    </div>
</nav>

<div class="container mt-4 p-4 rounded-lg" style="background-color: #282A35;">
    <form action="" method="post" class="needs-validation" novalidate>
        <div class="container p-3 mb-4 bg-light rounded-lg qn1">
            <div class="text-center mt-2">
                <h5>Total Score: {{data.score}}</h5>
            </div>
        </div>
        {{#with data.tut}}
        {{#if qns}}
        {{#each qns}}
        <div class="container p-4 mb-4 bg-light rounded-lg qn1">
            <div class="row">
                <div class="col d-flex align-items-center mt-2">
                    <h5>Question {{qnOrder}}</h5>
                </div>
            </div>
            <hr class="my-3 border border-dark" />
            <p class="lead">{{question}}</p>
            {{#if qnOption}}
            {{!-- {{#with qnOption}} --}}
            <fieldset class="form-group row" name="{{qnOrder}}">
                <div class="col-10">
                    {{#if answer}}
                    {{#with answer}}
                    {{#if input}}
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="{{id}}" id="option" value="">
                        <label class="form-check-label text-danger" for="option">
                            {{input}}
                        </label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="{{id}}" id="option1" value="">
                        <label class="form-check-label text-success" for="option1">
                            {{ans}}
                        </label>
                    </div>
                    {{else}}
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="{{id}}" id="option" value="">
                        <label class="form-check-label text-success" for="option">
                            {{ans}}
                        </label>
                    </div>
                    {{/if}}
                    {{/with}}
                    {{/if}}
                </div>
            </fieldset>
            {{!-- {{/with}} --}}
            {{else}}
            <fieldset class="form-group row">
                <div class="col-10">
                    {{#if answer}}
                    {{#with answer}}
                    {{#if input}}
                    <label for="ans" class="col-form-label h4 text-danger">Your Answer:</label>
                    <textarea type="text" class="form-control text-danger" id="ans" name="{{id}}"
                        required>{{input}}</textarea>
                    <label for="ans" class="col-form-label h4 text-success">Correct Answer:</label>
                    <textarea type="text" class="form-control text-success" id="ans" name="{{id}}"
                        required>{{ans}}</textarea>
                    {{else}}
                    <label for="ans" class="col-form-label h4 text-success">Your Answer:</label>
                    <textarea type="text" class="form-control text-success" id="ans" name="{{id}}"
                        required>{{ans}}</textarea>
                    {{/if}}
                    {{/with}}
                    {{/if}}
                </div>
            </fieldset>
            {{/if}}
        </div>
        {{/each}}
        {{else}}
        <div class="container p-4 mb-4 bg-light rounded-lg qn1">
            <h5 class="mt-1">No questions added.</h5>
        </div>
        {{/if}}
        {{/with}}

        <div class="d-flex justify-content-end submit">
            <button type="button" class="btn btn-outline-light mb-2"><a href="/student/content">Exit</a></button>
        </div>
    </form>
</div>